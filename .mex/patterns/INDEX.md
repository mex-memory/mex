# Pattern Index

Lookup table for project-specific pattern files.

| Pattern | Use when |
|---------|----------|
| `ui-surface.md` | Adding or extending a user-facing surface — a web view, an API endpoint, a CLI command — over the headless engine |
